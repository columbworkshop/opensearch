input { 
  beats {
    port => 5044
  }

  syslog {
    port => 5514
    type => syslog
  }

#   stdin { }
}

output {
  opensearch {
     hosts => ["https://os01:9200", "https://os02:9200"]
     index => "opensearch-logstash-docker-%{+YYYY.MM.dd}"
     user => "admin"
     password => "admin"
     ssl => true
     ssl_certificate_verification => false
   }
}
